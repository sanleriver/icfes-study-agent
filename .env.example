# Clave de Gemini para el proveedor real (dejar vacía para usar mock)
GEMINI_API_KEY=

# Proveedor de feedback: mock (desarrollo/tests, sin clave) | gemini
FEEDBACK_PROVIDER=mock

# Modelo usado por GeminiFeedbackProvider
GEMINI_MODEL=gemini-3.5-flash